<h1>Create Review</h1>
<div class="container">
  <%= simple_form_for [@restaurant, @review] do |f| %>
  <% f.hidden_field :restaurant_id, :value => @restaurant.id %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>
</div>
